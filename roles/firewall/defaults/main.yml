---
firewall_default_zone: "public"

firewall_allowed_ports:
  - "22/tcp"
  - "80/tcp"
  - "443/tcp"

firewall_blocked_services:
  - "dhcpv6-client"
  - "cockpit"

# Set to true only after reviewing an existing
# iptables/netfilter-persistent configuration.
firewall_remove_legacy_iptables: false
